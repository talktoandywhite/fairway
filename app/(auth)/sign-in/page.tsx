/**
 * Placeholder. The real sign-in / sign-up / magic-link / guardian-consent flow
 * arrives in Session 5. This exists so the `(auth)` group is a live route.
 */
export default function SignInPage() {
  return (
    <section className="flex flex-col gap-2">
      <h1 className="text-2xl font-semibold tracking-tight">Sign in</h1>
      <p className="text-sm text-muted-foreground">
        Authentication is not built yet — coming in Session 5.
      </p>
    </section>
  );
}
